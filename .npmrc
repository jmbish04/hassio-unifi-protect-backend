# Configuration for deployment environments
optional=false
package-manager-strict=false
legacy-peer-deps=true

# Skip problematic postinstall scripts in CI/CD
ignore-scripts=false

# Use npm for deployment if pnpm has issues
prefer-offline=true
